name: build signalforensics

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/checkout@v4
        with:
          repository: labcif/SignalForensics
          ref: 6d8c36ee4f317403d33c2d0357fcbcf7ee28d710
          path: SignalForensics

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: deps
        working-directory: SignalForensics
        run: |
          pip install poetry
          poetry config virtualenvs.in-project true
          poetry install
          poetry run pip install pyinstaller

      - name: build
        working-directory: SignalForensics
        run: >
          poetry run pyinstaller
          --noconfirm --clean --onefile --console
          --name SignalForensics
          --collect-all sqlcipher3
          --collect-all cryptography
          --collect-submodules modules
          --hidden-import sqlcipher3
          --hidden-import win32crypt
          --hidden-import win32api
          --hidden-import pywintypes
          --hidden-import pythoncom
          "--add-data=templates;templates"
          "--add-data=images;images"
          decrypt_signal.py

      - uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          files: SignalForensics/dist/SignalForensics.exe
